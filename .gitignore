# Local configuration with credentials — copy from the .sample.php files
config/database.php
config/google-oauth.php

# Logs (may contain API keys and call payloads)
logs/*
!logs/.gitkeep

# Claude Code local settings
.claude/settings.local.json

# Composer dependencies (run `composer install` on deploy)
vendor/

# OS files
.DS_Store
**/.DS_Store

# Uploads / downloads
html/downloads/*
!html/downloads/.gitkeep

# MaluDB REST API reference copy (not deployed from this repo)
v1/
